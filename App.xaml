<Application x:Class="WpfApp1.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfApp1"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style TargetType="Button">
            <!-- Основные свойства -->
            <Setter Property="Background" Value="#67BA80"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12 6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            
        </Style>
        
        <Color x:Key="PrimaryColor">#67BA80</Color>
        <Color x:Key="BackgroundColor">#FFFFFF</Color>
        <Color x:Key="BackgroundAltColor">#F4E8D3</Color>
        <Color x:Key="TextPrimaryColor">#1A1A1A</Color>
        <Color x:Key="ErrorColor">#E74C3C</Color>
        <Color x:Key="SuccessColor">#2ECC71</Color>
        <Color x:Key="WarningColor">#F39C12</Color>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <BitmapImage x:Key="Partner1Logo" UriSource="/Resources/Images/logo.png"/>

    </Application.Resources>
</Application>
